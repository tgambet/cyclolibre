import { MapExtensionDirective } from './map-extension.directive';

describe('MapExtensionDirective', () => {
  it('should create an instance', () => {
    const directive = new MapExtensionDirective();
    expect(directive).toBeTruthy();
  });
});
